# ffmpeg-static, installed only to transcode the media locally
node_modules/
package-lock.json

# untranscoded camera/render originals live outside the repo
*-original.mp4

# OS noise
.DS_Store
Thumbs.db
desktop.ini
.impeccable/


# Working notes. GitHub Pages serves every file on the branch, so anything
# committed here is publicly readable. These are not for that audience: the
# prompt logs document how the imagery was made, and the other two carry the
# open items on live claims and the answers prepared for client pushback.
hero-prompts.md
card-image-spec.md
hero-concept.md
NOTES.md
