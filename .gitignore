# ffmpeg-static, installed only to transcode the media locally
node_modules/
package-lock.json

# untranscoded camera/render originals live outside the repo
*-original.mp4

# OS noise
.DS_Store
Thumbs.db
desktop.ini
.impeccable/


# Internal positioning notes. GitHub Pages serves every file on the branch, so
# anything committed here is publicly readable — this one carries the answers to
# client pushback and is not for that audience.
hero-concept.md
